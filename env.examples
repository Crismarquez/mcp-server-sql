# Copy this file to .env and adjust values as needed

# Database type: databricks | postgres | azure | mock
DB_TYPE=mock

# Connection string for the selected DB_TYPE
# Leave empty for DB_TYPE=mock
DB_CONN_STR=

# --- Examples ---

# Databricks (databricks-sql-connector via SQLAlchemy)
# Replace <TOKEN>, <HOST>, <CATALOG>.<SCHEMA>
# Optionally include http_path if required by your workspace/warehouse
#DB_CONN_STR=databricks+connector://token:<TOKEN>@<HOST>/<CATALOG>.<SCHEMA>
# Example with http_path param:
#DB_CONN_STR=databricks+connector://token:<TOKEN>@<HOST>/<CATALOG>.<SCHEMA>?http_path=/sql/1.0/warehouses/<WAREHOUSE_ID>

# PostgreSQL (psycopg2)
#DB_CONN_STR=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME

# Azure SQL (SQL Server via ODBC Driver 18)
# If the driver name contains spaces, keep them URL-encoded with +
#DB_CONN_STR=mssql+pyodbc://USER:PASSWORD@SERVER.database.windows.net:1433/DBNAME?driver=ODBC+Driver+18+for+SQL+Server&Encrypt=yes&TrustServerCertificate=no


